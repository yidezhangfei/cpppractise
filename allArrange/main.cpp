#include <iostream>
#include <vector>

std::vector<std::vector<int>> arrange(int n) {
    std::vector<int> origin;
    for (int i = 1; i <= n; i++) {
        origin.push_back(i);
    }
    std::vector<std::vector<int>> result;
}

int main() {

}